<?php
/**
 * Task 10: FizzBuzz (The Classic — Now You Must Actually Write It)
 *
 * Write a function fizzBuzz($n) that:
 * - Prints numbers from 1 to $n
 * - Multiples of 3: print "Fizz"
 * - Multiples of 5: print "Buzz"
 * - Multiples of both: print "FizzBuzz"
 * - Otherwise: print the number
 *
 * Requirements:
 * - Must use a for loop
 * - Must NOT use if/elseif chain — use match expression
 * - Must NOT hardcode any numbers except 3 and 5
 * - Call with n=100
 *
 * Extension (mandatory):
 * Write a second version fizzBuzzExtended($n, $rules) where $rules is an
 * associative array like [3 => "Fizz", 5 => "Buzz", 7 => "Jazz"].
 * The function must work with any number of rules without changing its code.
 * Call with rules [3 => "Fizz", 5 => "Buzz", 7 => "Jazz"] and n=105.
 */

// Your solution below.
