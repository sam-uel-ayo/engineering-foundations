<?php
/**
 * Task 5: Array Destructuring
 *
 * PHP 7.1+ supports list() and the short [] syntax for destructuring.
 *
 * Requirements:
 * Part A: Destructure these arrays:
 *         $coordinates = [6.5244, 3.3792]; // Lagos coordinates
 *         Extract into $latitude and $longitude using [] syntax.
 *         Print: "Latitude: 6.5244, Longitude: 3.3792"
 *
 * Part B: Swap two variables without a temporary variable using list():
 *         $a = "hello"; $b = "world";
 *         Swap them in one line using list().
 *
 * Part C: Destructure in a foreach loop:
 *         $points = [[1,2], [3,4], [5,6], [7,8]];
 *         Use foreach with destructuring to print: "Point: (1, 2)" for each.
 *
 * Part D: Destructure associative arrays:
 *         $user = ["name" => "Samuel", "email" => "s@example.com", "role" => "admin"];
 *         Extract name and role (skip email) using array destructuring with keys.
 *
 * Part E: Return multiple values from a function using array destructuring.
 *         Write function getNameParts(string $fullName): array that returns
 *         [$firstName, $lastName] and destructure the result at the call site.
 */

// Your solution below.
