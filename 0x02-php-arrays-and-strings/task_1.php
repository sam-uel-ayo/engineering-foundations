<?php
/**
 * Task 1: Sorting Arrays
 *
 * PHP has many sort functions. Understanding which to use matters.
 *
 * Requirements:
 * Given: $scores = [85, 42, 96, 71, 58, 96, 33, 88, 71, 100];
 *
 * Part A: Sort ascending with sort() — print result
 * Part B: Sort descending with rsort() — print result
 * Part C: Sort but PRESERVE keys with asort() — print result, explain why keys matter
 * Part D: Given $users = [
 *           ["name" => "Zara", "age" => 25],
 *           ["name" => "Abel", "age" => 31],
 *           ["name" => "Alex", "age" => 23],
 *         ];
 *         Sort by name ascending using usort()
 *         Sort by age descending using usort()
 *         Print both results.
 *
 * Part E: Given $config = ["debug" => true, "version" => "1.0", "app" => "BuildBoard"];
 *         Sort by keys alphabetically with ksort()
 *         Sort by keys reverse with krsort()
 *         Print both results.
 *
 * Write a comment: "When would incorrect sorting choice cause a real bug?"
 */

// Your solution below.
