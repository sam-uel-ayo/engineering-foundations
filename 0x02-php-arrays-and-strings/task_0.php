<?php
/**
 * Task 0: Array Fundamentals
 *
 * Requirements:
 * Part A: Create an indexed array of 10 Nigerian cities.
 *         - Print the array with print_r
 *         - Print the count
 *         - Access the 3rd and last element
 *         - Add a city at the end
 *         - Add a city at the beginning
 *         - Remove the last city
 *         - Remove the first city
 *         Print the array after each operation.
 *
 * Part B: Create an associative array representing a user profile:
 *         name, email, age, role, created_at
 *         - Access each value by key
 *         - Check if key "email" exists
 *         - Check if key "phone" exists
 *         - Add a "phone" key
 *         - Update the "role" key
 *         - Delete the "age" key
 *         Print the array after each operation.
 *
 * Part C: Write a comment explaining the difference between
 *         isset(), array_key_exists(), and in_array().
 *         Give one real scenario where each is the right choice.
 */

// Your solution below.
