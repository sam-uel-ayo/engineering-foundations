<?php
/**
 * Task 0: Basic Functions
 *
 * Write three functions from scratch:
 *
 * 1. greet($name) — returns "Hello, [name]!"
 * 2. add($a, $b) — returns the sum of two numbers
 * 3. isEven($n) — returns true if n is even, false otherwise
 *
 * Requirements:
 * - All functions must have explicit return type declarations
 * - greet() must declare $name as string type
 * - add() must work with both int and float — use int|float union type
 * - isEven() must declare $n as int and return bool
 * - Call each function and print the results
 * - Write a comment explaining what return type declarations buy you
 */

// Your solution below.
