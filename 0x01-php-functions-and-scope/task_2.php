<?php
/**
 * Task 2: Variable Scope
 *
 * PHP handles scope differently from JavaScript. This task forces you
 * to understand exactly how PHP scope works.
 *
 * Requirements:
 * Part A: Create a global variable $counter = 0.
 *         Write a function incrementCounter() that tries to increment it.
 *         Call it 3 times. Print $counter after each call.
 *         What happens? Why? Write a comment explaining.
 *
 * Part B: Fix incrementCounter() using the global keyword.
 *         Call it 3 times again. Print $counter after each call.
 *
 * Part C: Write a function makeCounter() that uses a static variable
 *         to count how many times it has been called.
 *         Call it 5 times and print the count each time.
 *         Write a comment: "What is a static variable and when would I use one in a real app?"
 *
 * Part D: Write a comment comparing PHP scope to what you know about
 *         JavaScript scope. What is the biggest difference? (Minimum 3 sentences)
 */

// Your solution below.
