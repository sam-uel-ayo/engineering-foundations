<?php
/**
 * Task 1: Default Parameters
 *
 * Write a function createUser($name, $role = "viewer", $active = true, $score = 0.0)
 * that returns an associative array representing a user.
 *
 * Requirements:
 * - Return array must have keys: name, role, active, score, created_at
 * - created_at must be current timestamp as a formatted string: Y-m-d H:i:s
 * - Call the function 4 times:
 *   1. Only $name provided
 *   2. $name and $role provided
 *   3. $name, $role, $active provided
 *   4. All parameters provided
 * - Print each returned array using print_r()
 * - Write a comment explaining PHP's rule about default parameters position
 */

// Your solution below.
